import React from "react";
import ReactDOM from "react-dom/client";
import BankingSite from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BankingSite />
  </React.StrictMode>
);
